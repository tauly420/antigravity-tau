---
phase: 14-report-and-autolab-merge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/utils/pdf_renderer.py
  - backend/api/report.py
  - nixpacks.toml
autonomous: true
requirements: [D-07, D-08, D-09]

must_haves:
  truths:
    - "Results-only PDF endpoint returns a valid PDF with analysis summary, parameter table, and plots"
    - "WeasyPrint generates PDFs without 500 errors (locally and on Railway)"
    - "PDF output has proper academic formatting with correct fonts and margins"
  artifacts:
    - path: "backend/utils/pdf_renderer.py"
      provides: "assemble_results_html() function for results-only PDF"
      contains: "def assemble_results_html"
    - path: "backend/api/report.py"
      provides: "POST /api/report/export-results-pdf endpoint"
      contains: "@report_bp.route('/export-results-pdf'"
  key_links:
    - from: "backend/api/report.py"
      to: "backend/utils/pdf_renderer.py"
      via: "import assemble_results_html"
      pattern: "from utils.pdf_renderer import.*assemble_results_html"
---

<objective>
Build the results-only PDF endpoint and fix/improve the WeasyPrint PDF pipeline.

Purpose: D-09 requires two PDF export tiers. This plan adds the results-only PDF backend. D-07/D-08 require WeasyPrint debugging and quality improvements.
Output: Working `/api/report/export-results-pdf` endpoint + improved PDF quality in existing `/api/report/export-pdf`.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-report-and-autolab-merge-unified-page-for-full-report-or-analysis-only-mode-plus-fix-pdf-export/14-CONTEXT.md
@.planning/phases/14-report-and-autolab-merge-unified-page-for-full-report-or-analysis-only-mode-plus-fix-pdf-export/14-RESEARCH.md
@.planning/phases/14-report-and-autolab-merge-unified-page-for-full-report-or-analysis-only-mode-plus-fix-pdf-export/14-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add assemble_results_html() to pdf_renderer.py and export-results-pdf endpoint to report.py</name>
  <files>backend/utils/pdf_renderer.py, backend/api/report.py</files>
  <read_first>
    - backend/utils/pdf_renderer.py (full file -- understand assemble_report_html pattern, process_text_with_math, generate_pdf signatures)
    - backend/api/report.py (full file -- understand existing export-pdf endpoint pattern)
    - backend/templates/report_styles.css (understand existing CSS for PDF)
    - backend/templates/report_base.html (understand HTML template structure)
  </read_first>
  <action>
    **In backend/utils/pdf_renderer.py**, add a new function `assemble_results_html()` as a lightweight sibling to `assemble_report_html()`. Per D-09, this generates HTML for results-only PDF (no AI sections, no title page).

    Function signature:
    ```python
    def assemble_results_html(analysis_data, plots, summary='', language='he'):
    ```

    The function builds HTML containing:
    1. A heading: "Analysis Results" (en) or "תוצאות ניתוח" (he)
    2. The AI summary text (passed as `summary` string) in a styled box
    3. A parameter table built from `analysis_data['fit']['parameters']` -- each row: name, rounded value +/- uncertainty, LaTeX name. Include goodness-of-fit row (chi-squared/dof, R-squared, P-value) from `analysis_data['fit']['goodnessOfFit']`
    4. Formula calculation section (if `analysis_data.get('formula')` exists): expression, value +/- uncertainty
    5. N-sigma comparison (if `analysis_data.get('nsigma')` exists): n_sigma value, verdict, color-coded
    6. Plot images: fit plot and residuals plot as `<img>` tags from base64 data (same pattern as assemble_report_html -- write base64 to temp files, use file:// URIs)
    7. Figure captions below each plot: "Figure 1: Fit" and "Figure 2: Residuals"

    Use the same `_save_base64_image()` helper already used in `assemble_report_html()`. Wrap everything in `<div class="template-minimal">` for clean styling.

    **In backend/api/report.py**, add a new endpoint:

    ```python
    @report_bp.route('/export-results-pdf', methods=['POST'])
    def export_results_pdf():
    ```

    Request body JSON: `{ analysis_data: {...}, plots: {fit: "base64...", residuals: "base64..."}, summary: "...", language: "he"|"en" }`

    The endpoint:
    1. Extracts `analysis_data`, `plots`, `summary`, `language` from request JSON
    2. Calls `assemble_results_html(analysis_data, plots, summary, language)`
    3. Calls `process_text_with_math(html_body)` to convert LaTeX delimiters to KaTeX HTML
    4. Sets `direction = 'rtl' if language == 'he' else 'ltr'`
    5. Calls `generate_pdf(processed, direction=direction, lang=language)`
    6. Calls `_cleanup_temp_images()`
    7. Returns `Response(pdf_bytes, mimetype='application/pdf', headers={'Content-Disposition': 'attachment; filename="analysis-results.pdf"'})`
    8. Wraps in try/except returning `{"error": str(e), "traceback": traceback.format_exc()}, 500`

    Update the import line in report.py to include `assemble_results_html`:
    ```python
    from utils.pdf_renderer import assemble_report_html, assemble_results_html, generate_pdf, process_text_with_math, _cleanup_temp_images
    ```
  </action>
  <verify>
    <automated>cd "/Users/uri/uri_labtools/try1/uri_labtools/Google antigravity /Tau-ly website/backend" && python -c "from utils.pdf_renderer import assemble_results_html; print('import OK')" && python -m pytest tests/test_pdf_render.py -x --tb=short 2>/dev/null; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - backend/utils/pdf_renderer.py contains `def assemble_results_html(`
    - backend/api/report.py contains `@report_bp.route('/export-results-pdf'`
    - backend/api/report.py contains `from utils.pdf_renderer import` with `assemble_results_html` in the import list
    - `python -c "from utils.pdf_renderer import assemble_results_html"` exits 0
    - assemble_results_html function handles missing optional fields (no formula, no nsigma) without errors
  </acceptance_criteria>
  <done>Results-only PDF endpoint exists and returns valid PDF bytes when called with analysis data</done>
</task>

<task type="auto">
  <name>Task 2: Debug and fix WeasyPrint PDF generation + improve PDF quality</name>
  <files>backend/utils/pdf_renderer.py, backend/templates/report_styles.css, nixpacks.toml</files>
  <read_first>
    - backend/utils/pdf_renderer.py (understand generate_pdf function, font paths, KaTeX rendering)
    - backend/templates/report_styles.css (current PDF styling)
    - backend/templates/report_base.html (HTML template)
    - nixpacks.toml (Railway system dependencies)
  </read_first>
  <action>
    Per D-07 and D-08: Debug the WeasyPrint pipeline and improve PDF output quality.

    **Step 1: Local validation.** Run the test-pdf endpoint locally:
    ```python
    cd backend && python -c "from utils.pdf_renderer import generate_test_pdf; pdf = generate_test_pdf(); open('/tmp/test.pdf','wb').write(pdf); print(f'OK: {len(pdf)} bytes')"
    ```
    If this fails, fix the local issue first (likely font paths or KaTeX subprocess).

    **Step 2: Diagnose Railway issue.** The WeasyPrint 500 error on Railway (D-07) could be caused by:
    - Missing `GDK_PIXBUF_MODULE_FILE` env var in nixpacks
    - Missing `XDG_DATA_DIRS` for shared-mime-info
    - KaTeX subprocess (npx katex) not in PATH during Python execution
    - Font file paths not resolving correctly

    In `nixpacks.toml`, ensure the `[phases.setup]` or `[start]` section sets required env vars. Add to the start command or env section:
    ```toml
    [variables]
    GDK_PIXBUF_MODULE_FILE = ""
    ```
    If the issue is KaTeX subprocess, ensure the `render_latex_for_pdf()` function in pdf_renderer.py has a robust fallback -- if `npx katex` fails, use `markdown_katex` Python package, and if that also fails, return the raw LaTeX wrapped in `<code>` tags rather than crashing.

    **Step 3: PDF quality improvements (D-08).** In `backend/templates/report_styles.css`:
    - Ensure `@page { size: A4; margin: 2.5cm 2cm 2.5cm 2cm; }` for proper academic margins
    - Set `body { font-family: 'Inter', 'Noto Sans Hebrew', sans-serif; font-size: 11pt; line-height: 1.6; }`
    - Ensure parameter table has proper borders: `border-collapse: collapse; width: 100%;` with `td, th { border: 1px solid #ccc; padding: 6px 10px; }`
    - Ensure plot images: `max-width: 100%; height: auto;` with `figure { text-align: center; margin: 1em 0; }` and `figcaption { font-size: 10pt; color: #555; margin-top: 4px; }`
    - Ensure section headers have proper spacing: `h2 { margin-top: 1.5em; margin-bottom: 0.5em; font-size: 14pt; }`
    - Add `.results-summary { background: #f5f5f5; border-left: 4px solid #2e7d32; padding: 12px 16px; margin-bottom: 1em; font-size: 11pt; }`

    **Step 4: Ensure generate_pdf error messages are descriptive.** If WeasyPrint import fails, catch ImportError and return a clear message: "WeasyPrint not installed. Install with: pip install weasyprint"
  </action>
  <verify>
    <automated>cd "/Users/uri/uri_labtools/try1/uri_labtools/Google antigravity /Tau-ly website/backend" && python -c "
from utils.pdf_renderer import generate_test_pdf, assemble_results_html, process_text_with_math, generate_pdf
# Test 1: test-pdf still works
pdf = generate_test_pdf()
assert len(pdf) > 1000, f'Test PDF too small: {len(pdf)} bytes'
# Test 2: results-only PDF works
html = assemble_results_html(
    analysis_data={'fit': {'parameters': [{'name': 'a', 'value': 4.9, 'uncertainty': 0.1, 'rounded': '4.9 +/- 0.1', 'latex': 'a'}], 'goodnessOfFit': {'chiSquaredReduced': 1.2, 'rSquared': 0.99, 'pValue': 0.3, 'dof': 8}}, 'summary': 'Test summary'},
    plots={},
    summary='Test analysis summary',
    language='en'
)
assert 'Test analysis summary' in html
processed = process_text_with_math(html)
pdf2 = generate_pdf(processed, direction='ltr', lang='en')
assert len(pdf2) > 500, f'Results PDF too small: {len(pdf2)} bytes'
print('All PDF tests passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - `generate_test_pdf()` returns PDF bytes > 1000 bytes without errors
    - `assemble_results_html()` produces HTML containing the summary text
    - `generate_pdf()` produces valid PDF from results HTML
    - report_styles.css contains `@page` rule with A4 size
    - report_styles.css contains `.results-summary` class
    - pdf_renderer.py has fallback handling if KaTeX subprocess fails
  </acceptance_criteria>
  <done>WeasyPrint generates valid PDFs locally; results-only and full-report PDFs both render with improved formatting; Railway deployment issues addressed in nixpacks.toml</done>
</task>

</tasks>

<verification>
- `python -c "from utils.pdf_renderer import assemble_results_html, generate_test_pdf"` imports without error
- POST to `/api/report/export-results-pdf` with test data returns 200 + PDF content-type
- POST to `/api/report/export-pdf` with test data returns 200 + PDF content-type
- Both PDF outputs open correctly in a PDF reader
</verification>

<success_criteria>
- Results-only PDF endpoint exists and returns valid A4 PDF with parameter table, plots, and summary
- Full report PDF pipeline works without 500 errors
- PDF quality improved (proper margins, fonts, table formatting)
</success_criteria>

<output>
After completion, create `.planning/phases/14-report-and-autolab-merge-unified-page-for-full-report-or-analysis-only-mode-plus-fix-pdf-export/14-01-SUMMARY.md`
</output>
