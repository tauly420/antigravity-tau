---
phase: quick
plan: 260405-na0
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prompts/report_system.py
  - backend/api/report.py
  - backend/utils/docx_renderer.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "AI generates a results section with formula/nsigma prose"
    - "HTML parameter table appears at top of results section in preview"
    - "DOCX export renders the HTML parameter table as a Word table"
    - "Discussion section no longer restates fit parameter values"
  artifacts:
    - path: "backend/prompts/report_system.py"
      provides: "build_results_table_html function + updated JSON schema with results key"
      contains: "def build_results_table_html"
    - path: "backend/api/report.py"
      provides: "results in required_keys, HTML table injection"
      contains: "results"
    - path: "backend/utils/docx_renderer.py"
      provides: "HTML table parsing for results section"
      contains: "_add_html_table_to_doc"
  key_links:
    - from: "backend/api/report.py"
      to: "backend/prompts/report_system.py"
      via: "import build_results_table_html"
      pattern: "from prompts.report_system import.*build_results_table_html"
    - from: "backend/utils/docx_renderer.py"
      to: "generate_docx results section"
      via: "HTML table detection in results content"
      pattern: "<table"
---

<objective>
Add a "results" section to AI-generated lab reports. The results section has two parts: (1) an HTML parameter table built from analysis_data (not AI-generated), injected at the top, and (2) AI-written prose covering formula calculations and n-sigma comparisons.

Purpose: Students need a proper results section showing measured parameters in a table, with formula derivations and comparisons to theory — separate from the discussion section which should focus on interpretation.

Output: Three modified backend files enabling results section generation, HTML table injection, and DOCX table rendering.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/prompts/report_system.py
@backend/api/report.py
@backend/utils/docx_renderer.py
@frontend/src/components/report/ReportSection.tsx (read-only — confirms results already in SECTION_ORDER, no changes needed)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add build_results_table_html and update prompt schema in report_system.py</name>
  <files>backend/prompts/report_system.py</files>
  <action>
  1. Add a `build_results_table_html(analysis_data: dict) -> str` function (module-level, not prefixed with underscore so it's importable):
     - If analysis_data is empty or has no fit data with parameters, return empty string ""
     - Extract fit parameters from `analysis_data["fit"]["parameters"]` (list of dicts with name/value/uncertainty/rounded)
     - Build HTML table with columns: Quantity | Rounded | Full Precision
     - For each parameter: name in col 1, rounded value (from p["rounded"]) in col 2, full precision `{value} +/- {uncertainty}` in col 3
     - Add chi-squared reduced row if `analysis_data["fit"]["goodnessOfFit"]["chiSquaredReduced"]` exists (format to 4 decimal places)
     - Add P-value row if `analysis_data["fit"]["goodnessOfFit"]["pValue"]` exists (format to 4 decimal places)
     - Table HTML style: `border-collapse: collapse; width: 100%; margin-bottom: 1em;`, cells: `border: 1px solid #ccc; padding: 6px 12px; text-align: left;`, header row: `background: #e8f0fe; font-weight: bold;`
     - Use `<table>`, `<thead>`, `<tbody>`, `<tr>`, `<th>`, `<td>` tags (standard HTML)

  2. Update `build_report_system_prompt`:
     - Change "Generate 4 sections" to "Generate 5 sections"
     - Add results section instruction between method and discussion:
       `3. results - Results narrative: Write ONLY about formula calculations and n-sigma comparison. If formula data exists, write a lead-in sentence then the formula on a new line using $$...$$ display math (e.g., "We calculated the period using the formula:\n\n$$T = \\frac{2\\pi}{\\omega}$$\n\nGiving T = 2.51 +/- 0.03 s."). If n-sigma data exists, state the comparison result. If neither formula nor nsigma data exists, write one brief sentence noting the fit parameters are presented in the table above. Do NOT restate individual parameter values or chi-squared — those are in the results table.`
     - Renumber discussion to 4, conclusions to 5
     - Update discussion instruction: `4. discussion - Discussion: interpret the ACTUAL results above focusing on physical meaning, compare to expected physics, identify dominant error sources, discuss experimental limitations. Do NOT restate fit parameter values or chi-squared — those are already in the results table.`
     - Update JSON schema line to: `{"theory": "...", "method": "...", "results": "...", "discussion": "...", "conclusions": "...", "warnings": ["..."]}`
  </action>
  <verify>
    <automated>cd "/Users/uri/uri_labtools/try1/uri_labtools/Google antigravity /Tau-ly website" && python3 -c "
from backend.prompts.report_system import build_results_table_html, build_report_system_prompt
# Test empty data returns empty string
assert build_results_table_html({}) == ''
assert build_results_table_html(None) == ''
# Test with fit data
data = {'fit': {'parameters': [{'name': 'a', 'value': 1.234, 'uncertainty': 0.056, 'rounded': '1.23 +/- 0.06'}], 'goodnessOfFit': {'chiSquaredReduced': 1.2345}}}
html = build_results_table_html(data)
assert '<table' in html
assert '1.23 +/- 0.06' in html
assert '1.234 +/- 0.056' in html
# Test prompt has results key
prompt = build_report_system_prompt({}, '', data, [])
assert 'results' in prompt
assert '\"results\"' in prompt
print('ALL PASS')
"
    </automated>
  </verify>
  <done>build_results_table_html returns HTML table from analysis_data, prompt schema includes "results" key, discussion instructions updated to not restate parameters</done>
</task>

<task type="auto">
  <name>Task 2: Update report.py to include results in required_keys and inject HTML table</name>
  <files>backend/api/report.py</files>
  <action>
  1. Update the import line to also import build_results_table_html:
     `from prompts.report_system import build_report_system_prompt, build_results_table_html`

  2. In the `generate()` endpoint, update `required_keys`:
     `required_keys = {"theory", "method", "results", "discussion", "conclusions"}`

  3. After `sections = json.loads(response.choices[0].message.content)` and after the required_keys validation, inject the HTML table:
     ```python
     table_html = build_results_table_html(analysis_data)
     if table_html:
         sections["results"] = table_html + "\n" + sections.get("results", "")
     ```

  4. Add "results" to the response JSON:
     ```python
     return jsonify({
         "sections": {
             "theory": sections["theory"],
             "method": sections["method"],
             "results": sections["results"],
             "discussion": sections["discussion"],
             "conclusions": sections["conclusions"],
             "warnings": sections.get("warnings", []),
         },
         "error": None,
     })
     ```
  </action>
  <verify>
    <automated>cd "/Users/uri/uri_labtools/try1/uri_labtools/Google antigravity /Tau-ly website" && python3 -c "
from backend.api.report import report_bp
# Verify the blueprint registered correctly (no import errors)
routes = [rule.rule for rule in report_bp.deferred_functions] if hasattr(report_bp, 'deferred_functions') else []
print('Import OK')
# Verify build_results_table_html is importable from report module context
from prompts.report_system import build_results_table_html
print('Import build_results_table_html OK')
print('ALL PASS')
"
    </automated>
  </verify>
  <done>required_keys includes "results", HTML table injected at top of results section, response JSON includes results field</done>
</task>

<task type="auto">
  <name>Task 3: Add HTML table parsing to DOCX renderer for results section</name>
  <files>backend/utils/docx_renderer.py</files>
  <action>
  1. Add `import re` at top if not already present (it is), and add `from html.parser import HTMLParser` import.

  2. Add a helper class `_HTMLTableParser(HTMLParser)` that parses simple HTML tables:
     - Tracks current tag (table/thead/tbody/tr/th/td)
     - Collects rows as list of lists of strings
     - Tracks whether current row is header (inside thead or contains th)
     - `get_rows()` returns `(header_row: list[str], data_rows: list[list[str]])`

  3. Add `_add_html_table_to_doc(doc, html_str, is_rtl=True)` function:
     - Parse html_str using _HTMLTableParser
     - Get header_row and data_rows
     - Create python-docx Table with len(header_row) columns (default 3)
     - Header row: bold text, light blue shading (hex "DDEEFF") via `<w:shd w:val="clear" w:fill="DDEEFF"/>` on each cell
     - Data rows: plain styling
     - Set font on all cells using `_set_run_font`
     - If is_rtl, set RTL on cell paragraphs

  4. Modify `_add_results_section` in `generate_docx` flow:
     - The `generate_docx` function currently calls `_add_results_section(doc, analysis_data, plots, language, is_rtl)` which builds the table from analysis_data directly
     - Now, `generate_docx` should also receive `sections` dict. Check if `sections.get("results", "")` starts with `<table` (case-insensitive strip)
     - If it does: split on first `</table>` to get html_table_part and remaining_prose
       - Call `_add_html_table_to_doc(doc, html_table_part + "</table>", is_rtl)` for the table
       - Call `add_mixed_paragraph` for each paragraph of remaining_prose (skip empty)
       - Skip the old `_add_results_section` parameter table logic (plots still rendered)
     - If results content does NOT start with `<table`: fall back to existing `_add_results_section` behavior

  5. Update `generate_docx` signature and body:
     - Already receives `sections` dict — use `sections.get("results", "")` to detect HTML table
     - After heading for results section, check for HTML table in sections["results"]
     - If HTML table found: render it via `_add_html_table_to_doc`, then render remaining prose via `add_mixed_paragraph`, then render formula/nsigma/plots from analysis_data (skip the parameter table part of `_add_results_section` since the HTML table replaces it)
     - Simplest approach: in `generate_docx`, replace the `_add_results_section` call with inline logic that:
       a. Adds the results heading
       b. Checks `sections.get("results", "")` for `<table` prefix
       c. If found: parse and render HTML table, then render remaining prose, then render plots from analysis_data
       d. If not found: call existing `_add_results_section` as fallback
  </action>
  <verify>
    <automated>cd "/Users/uri/uri_labtools/try1/uri_labtools/Google antigravity /Tau-ly website" && python3 -c "
from backend.utils.docx_renderer import generate_docx, _add_html_table_to_doc
from docx import Document
# Test HTML table parser + doc insertion
doc = Document()
html = '<table><thead><tr><th>Qty</th><th>Rounded</th><th>Full</th></tr></thead><tbody><tr><td>a</td><td>1.23</td><td>1.234</td></tr></tbody></table>'
_add_html_table_to_doc(doc, html, is_rtl=False)
assert len(doc.tables) == 1
assert len(doc.tables[0].rows) == 2  # header + 1 data row
print('Table parsing OK')

# Test generate_docx with results section containing HTML table
sections = {
    'theory': 'test theory',
    'method': 'test method',
    'results': html + '\nSome prose about formula.',
    'discussion': 'test discussion',
    'conclusions': 'test conclusions',
}
title_page = {'experimentTitle': 'Test'}
result = generate_docx(sections, title_page, {}, {}, language='en')
assert len(result) > 0
print('DOCX generation OK')
print('ALL PASS')
"
    </automated>
  </verify>
  <done>DOCX renderer parses HTML table from results section content, renders as styled Word table with blue header, falls back to existing behavior when no HTML table present</done>
</task>

</tasks>

<verification>
1. `build_results_table_html({})` returns empty string
2. `build_results_table_html(valid_data)` returns HTML with `<table>` containing parameter rows
3. `build_report_system_prompt` output contains "results" in JSON schema
4. `report.py` generate endpoint includes "results" in required_keys and response
5. DOCX renderer correctly parses HTML table and creates Word table
6. Existing DOCX generation without HTML table still works (fallback path)
</verification>

<success_criteria>
- AI returns 5 sections (theory, method, results, discussion, conclusions) instead of 4
- Results section in browser preview shows HTML parameter table followed by formula/nsigma prose
- DOCX export renders the parameter table as a native Word table with blue header row
- Discussion section focuses on interpretation, not restating parameter values
</success_criteria>

<output>
After completion, create `.planning/quick/260405-na0-add-results-section-to-lab-report-with-h/260405-na0-SUMMARY.md`
</output>
