---
phase: 13-ui-overhaul
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - frontend/src/components/Home.tsx
  - frontend/src/styles/global.css
autonomous: false
requirements: [D-01, D-02, D-03]

must_haves:
  truths:
    - "Homepage shows an intro paragraph describing what Tau-LY offers"
    - "AutoLab hero card is visually larger and more prominent than other tool cards"
    - "Tool cards appear in a grid below the hero card"
    - "Clicking AutoLab hero card navigates to /autolab"
    - "Clicking any tool card navigates to its feature page"
    - "Homepage looks cohesive on dark theme background"
  artifacts:
    - path: "frontend/src/components/Home.tsx"
      provides: "Homepage with intro, hero card, and tool grid"
      contains: "home-hero-card"
    - path: "frontend/src/styles/global.css"
      provides: "Homepage CSS classes for intro, hero, and grid"
      contains: "home-hero-card"
  key_links:
    - from: "frontend/src/components/Home.tsx"
      to: "/autolab"
      via: "Link component on hero card"
      pattern: 'to="/autolab"'
    - from: "frontend/src/components/Home.tsx"
      to: "frontend/src/styles/global.css"
      via: "className references for home-intro, home-hero-card, home-tools-heading"
      pattern: "home-hero-card"
---

<objective>
Redesign the homepage with an intro section, a prominent AutoLab hero card, and a restructured tool grid below.

Purpose: Transform the homepage from a flat tool grid into a product landing page that highlights AutoLab as the flagship feature (per D-01, D-02, D-03).
Output: Home.tsx with three-section layout (intro, hero, grid) and matching CSS classes in global.css.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ui-overhaul-new-homepage-and-removal-of-sidebar-each-feature-accessible-from-home-page-and-a-back-to-home-button/13-CONTEXT.md
@.planning/phases/13-ui-overhaul-new-homepage-and-removal-of-sidebar-each-feature-accessible-from-home-page-and-a-back-to-home-button/13-UI-SPEC.md
@.planning/phases/13-ui-overhaul-new-homepage-and-removal-of-sidebar-each-feature-accessible-from-home-page-and-a-back-to-home-button/13-01-SUMMARY.md

<interfaces>
<!-- From 13-UI-SPEC.md — copywriting contract -->
Intro heading: "Your Physics Lab, Automated"
Intro paragraph: "Tau-LY gives physics and engineering students a complete toolkit for lab work -- from curve fitting and uncertainty propagation to AI-driven analysis that turns raw data into publication-ready results."
Hero card title: "AutoLab"
Hero card description: "Upload your data, describe what you need in plain language, and get a complete physics analysis -- fit parameters, uncertainties, plots, and comparison to theory."
Hero CTA: "Start Analysis" followed by right arrow

<!-- From Home.tsx — existing tools array (lines 4-93), to be kept for the grid below the hero -->
tools array has 11 items: Lab Workflow, Graph Fitting, Formula Calculator, N-sigma Calculator, Matrix Calculator, ODE Solver, Numerical Integration, Unit Converter, Fourier Analysis, Statistics Calculator, Constants Reference

<!-- From global.css — existing .tool-card class (lines 358-390) is already styled and will be reused for the grid cards -->

<!-- Color tokens (dark theme active after Plan 01): -->
--bg: #1a1a2e (page background)
--surface: #16213e (tool cards)
--surface-elevated: #243356 (hero card)
--text: #e8e8f0 (headings)
--text-secondary: #c0c0d4 (descriptions)
--text-muted: #a8a8c0 ("More Tools" label)
--primary: #4dd0e1 (teal — hero CTA, interactive)
--accent: #42a5f5 (blue — card borders, structural)
--border: #2a2a4a
--radius: 12px
--transition: 0.2s cubic-bezier(0.4, 0, 0.2, 1)
--shadow-lg: 0 8px 30px rgba(0,0,0,0.35), 0 4px 10px rgba(0,0,0,0.2)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure Home.tsx with intro, hero card, and tool grid</name>
  <files>frontend/src/components/Home.tsx</files>
  <read_first>
    - frontend/src/components/Home.tsx (full file — existing tools array and grid layout)
    - .planning/phases/13-ui-overhaul-new-homepage-and-removal-of-sidebar-each-feature-accessible-from-home-page-and-a-back-to-home-button/13-UI-SPEC.md (Copywriting Contract table and Component Inventory table)
  </read_first>
  <action>
Rewrite the `Home` component to have three sections inside `home-container` (per D-01):

**Keep the existing `tools` array** (lines 4-93) unchanged. It still powers the tool grid.

**Replace the return JSX** with this structure:

```tsx
function Home() {
    const { setCurrentTool } = useAnalysis();

    return (
        <div className="home-container">
            {/* Section 1: Intro (per D-02) */}
            <section className="home-intro">
                <h2>Your Physics Lab, Automated</h2>
                <p>
                    Tau-LY gives physics and engineering students a complete toolkit
                    for lab work &mdash; from curve fitting and uncertainty propagation
                    to AI-driven analysis that turns raw data into publication-ready results.
                </p>
            </section>

            {/* Section 2: AutoLab Hero Card (per D-03) */}
            <Link
                to="/autolab"
                onClick={() => setCurrentTool('AutoLab')}
                className="home-hero-link"
            >
                <div className="home-hero-card">
                    <div className="home-hero-icon">🤖</div>
                    <div className="home-hero-content">
                        <h2>AutoLab</h2>
                        <p>
                            Upload your data, describe what you need in plain language,
                            and get a complete physics analysis &mdash; fit parameters,
                            uncertainties, plots, and comparison to theory.
                        </p>
                        <span className="home-hero-cta">
                            Start Analysis &rarr;
                        </span>
                    </div>
                </div>
            </Link>

            {/* Section 3: Tool Grid */}
            <h3 className="home-tools-heading">More Tools</h3>
            <div className="home-tools-grid">
                {tools.map(tool => (
                    <Link
                        key={tool.path}
                        to={tool.path}
                        onClick={() => setCurrentTool(tool.title)}
                        style={{ textDecoration: 'none', color: 'inherit' }}
                    >
                        <div className="tool-card" style={{
                            borderLeft: `4px solid ${tool.color}`,
                        }}>
                            <div style={{ display: 'flex', alignItems: 'center', gap: '0.75rem', marginBottom: '0.75rem' }}>
                                {tool.icon ? (
                                    <img
                                        src={tool.icon}
                                        alt={tool.title}
                                        style={{ width: '40px', height: '40px', objectFit: 'contain', borderRadius: '6px' }}
                                        onError={(e) => {
                                            (e.target as HTMLImageElement).style.display = 'none';
                                            const next = (e.target as HTMLImageElement).nextElementSibling;
                                            if (next) (next as HTMLElement).style.display = 'flex';
                                        }}
                                    />
                                ) : null}
                                <span className="tool-emoji" style={{
                                    display: tool.icon ? 'none' : 'flex',
                                    width: '40px', height: '40px',
                                    alignItems: 'center', justifyContent: 'center',
                                    fontSize: '1.6rem',
                                    background: `${tool.color}15`,
                                    borderRadius: '8px',
                                    flexShrink: 0,
                                }}>{tool.emoji}</span>
                                <h3 style={{ margin: 0, color: 'var(--text)' }}>{tool.title}</h3>
                            </div>
                            <p style={{ color: 'var(--text-secondary)', fontSize: '0.9rem', margin: 0, lineHeight: 1.5 }}>
                                {tool.desc}
                            </p>
                        </div>
                    </Link>
                ))}
            </div>
        </div>
    );
}
```

Key details:
- The intro text and hero card copy come from the UI-SPEC Copywriting Contract — use them exactly
- AutoLab hero card uses `className="home-hero-link"` on the Link and `className="home-hero-card"` on the inner div
- The existing tool card rendering (with img/emoji fallback pattern) is preserved exactly
- Remove the old inline `style` on the grid container div — the new `home-tools-grid` class handles layout
- The `home-tools-heading` is an h3 with "More Tools" text above the grid
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit && grep -q 'home-hero-card' src/components/Home.tsx && grep -q 'home-intro' src/components/Home.tsx && grep -q 'Your Physics Lab, Automated' src/components/Home.tsx && grep -q 'Start Analysis' src/components/Home.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Home.tsx contains `className="home-intro"` section
    - Home.tsx contains the exact string "Your Physics Lab, Automated" as the intro heading
    - Home.tsx contains `className="home-hero-card"` div
    - Home.tsx contains `className="home-hero-link"` Link wrapping the hero card
    - Home.tsx contains `to="/autolab"` on the hero card Link
    - Home.tsx contains `className="home-hero-cta"` span with "Start Analysis" text
    - Home.tsx contains `className="home-tools-heading"` with "More Tools"
    - Home.tsx contains `className="home-tools-grid"` div wrapping the tool.map()
    - Home.tsx still contains the `tools` array with 11 items
    - Home.tsx still imports `useAnalysis` and calls `setCurrentTool`
    - TypeScript compilation succeeds with zero errors
  </acceptance_criteria>
  <done>Homepage has three-section layout: intro paragraph, AutoLab hero card with CTA, and labeled tool grid. All copy matches UI-SPEC. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add homepage CSS classes to global.css</name>
  <files>frontend/src/styles/global.css</files>
  <read_first>
    - frontend/src/styles/global.css (full file — to find correct insertion point after the Tool Cards section around line 390, and to see existing .tool-card styles)
    - .planning/phases/13-ui-overhaul-new-homepage-and-removal-of-sidebar-each-feature-accessible-from-home-page-and-a-back-to-home-button/13-UI-SPEC.md (Component Inventory table with exact CSS specs for each new class)
  </read_first>
  <action>
Add the following CSS classes to global.css. Insert them right after the existing `/* Tool Cards (Home) */` section (after the `.tool-card:hover::before` rule).

```css
/* ═════════════ Homepage Layout ═════════════ */
.home-container {
    padding: 64px 16px;
}

.home-intro {
    text-align: center;
    max-width: 640px;
    margin: 0 auto;
    padding: 0 16px;
}

.home-intro h2 {
    font-size: 1.625rem;
    font-weight: 700;
    line-height: 1.2;
    color: var(--text);
    margin: 0 0 12px;
}

.home-intro p {
    font-size: 0.9375rem;
    font-weight: 400;
    line-height: 1.65;
    color: var(--text-secondary);
    margin: 0;
}

/* ═════════════ AutoLab Hero Card ═════════════ */
.home-hero-link {
    text-decoration: none;
    color: inherit;
    display: block;
    margin: 48px auto 0;
    max-width: 640px;
}

.home-hero-card {
    background: var(--surface-elevated);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 32px;
    transition: transform var(--transition), box-shadow var(--transition), border-color var(--transition);
}

.home-hero-card:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
    border-color: var(--primary);
}

.home-hero-icon {
    font-size: 2.5rem;
    margin-bottom: 16px;
}

.home-hero-content h2 {
    font-size: 1.625rem;
    font-weight: 700;
    line-height: 1.2;
    color: var(--text);
    margin: 0;
}

.home-hero-content p {
    font-size: 0.9375rem;
    font-weight: 400;
    line-height: 1.65;
    color: var(--text-secondary);
    margin: 8px 0 16px;
}

.home-hero-cta {
    font-size: 0.875rem;
    font-weight: 400;
    color: var(--primary);
    display: inline-flex;
    align-items: center;
    gap: 8px;
}

/* ═════════════ Tool Grid ═════════════ */
.home-tools-heading {
    font-size: 0.875rem;
    font-weight: 400;
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin: 48px 0 16px;
    padding: 0 16px;
}

.home-tools-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1.5rem;
    padding: 0 1rem;
}
```

Also add responsive rules inside the existing `@media (max-width: 600px)` block:

```css
.home-container {
    padding: 32px 8px;
}

.home-hero-link {
    margin-top: 32px;
}

.home-hero-card {
    padding: 24px;
}

.home-tools-heading {
    margin-top: 32px;
}
```

These new classes use ONLY existing CSS tokens (var(--surface-elevated), var(--primary), var(--text), etc.) — no hardcoded colors except where the UI-SPEC explicitly specifies them (rgba for separator). The values match the UI-SPEC Component Inventory table exactly.
  </action>
  <verify>
    <automated>cd frontend && npm run build && grep -q 'home-hero-card' src/styles/global.css && grep -q 'home-intro' src/styles/global.css && grep -q 'home-hero-cta' src/styles/global.css && grep -q 'home-tools-grid' src/styles/global.css && grep -q 'home-tools-heading' src/styles/global.css && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - global.css contains `.home-container` with `padding: 64px 16px`
    - global.css contains `.home-intro` with `text-align: center` and `max-width: 640px`
    - global.css contains `.home-hero-link` with `max-width: 640px` and `margin: 48px auto 0`
    - global.css contains `.home-hero-card` with `background: var(--surface-elevated)` and `padding: 32px`
    - global.css contains `.home-hero-card:hover` with `border-color: var(--primary)`
    - global.css contains `.home-hero-cta` with `color: var(--primary)` and `display: inline-flex`
    - global.css contains `.home-tools-heading` with `text-transform: uppercase` and `color: var(--text-muted)`
    - global.css contains `.home-tools-grid` with `grid-template-columns: repeat(auto-fit, minmax(280px, 1fr))`
    - Full Vite build (`npm run build`) succeeds with zero errors
  </acceptance_criteria>
  <done>All homepage CSS classes added matching UI-SPEC exactly. Responsive variants included. Build passes. Homepage renders with intro, hero, and grid sections properly styled on dark theme.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete UI overhaul</name>
  <what-built>
    Complete UI overhaul: sidebar removed, dark theme active, route-aware header with back button, homepage with intro section, AutoLab hero card, and tool grid.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `cd frontend && npm run dev` (or use `./start.sh` from project root)
    2. Open http://localhost:5173 in browser
    3. **Homepage checks:**
       - Page has dark navy background (not white/light)
       - No left sidebar visible anywhere
       - Header shows "Tau-LY Lab Tools" with logo
       - Below header: intro section with "Your Physics Lab, Automated" heading
       - Large AutoLab hero card with robot emoji, description, and "Start Analysis" CTA in teal
       - "MORE TOOLS" label in muted uppercase text
       - Grid of 11 tool cards below
       - Hover over hero card — should lift with shadow and teal border
       - Hover over tool cards — should lift with shadow
    4. **Navigation checks:**
       - Click AutoLab hero card — navigates to /autolab
       - Header now shows: left arrow "Home" | pipe | "Tau-LY -- AutoLab"
       - Click "Home" back button — returns to homepage
       - Navigate to any other tool (e.g., click Formula Calculator card)
       - Header shows: left arrow "Home" | "Tau-LY -- Formula"
       - Click the Tau-LY logo — returns to homepage
    5. **AI panel check:**
       - The floating "Ask AI" button still appears (bottom-right)
       - Clicking it opens the right-side chat panel as before
    6. **Dark theme coherence:**
       - No jarring white boxes or mismatched backgrounds
       - Text is readable (light on dark)
       - Footer has dark background with muted text
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` — Full build must succeed
2. Homepage renders three sections: intro, hero card, tool grid
3. Hero card is visually prominent with elevated background
4. All navigation paths work (home, feature pages, back button)
5. Dark theme renders cohesively across all visible elements
</verification>

<success_criteria>
- Homepage has intro paragraph, AutoLab hero card, and tool grid in that order
- AutoLab hero card is the most visually prominent element
- Dark theme colors are cohesive (no white backgrounds, readable text)
- All tool cards navigate to their feature pages
- User approves visual appearance in checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/13-ui-overhaul-new-homepage-and-removal-of-sidebar-each-feature-accessible-from-home-page-and-a-back-to-home-button/13-02-SUMMARY.md`
</output>
