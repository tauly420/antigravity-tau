---
phase: 02-theme-token-system
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - frontend/src/styles/global.css
  - frontend/src/App.tsx
autonomous: false
requirements:
  - THEME-01

must_haves:
  truths:
    - "App root element applies dark theme by default"
    - "All hardcoded colors in CSS class rules use var() token references"
    - "Body and html get dark theme attribute for full coverage"
    - "The app renders visually in dark theme with scientific instrument aesthetic"
  artifacts:
    - path: "frontend/src/styles/global.css"
      provides: "All component CSS rules use var() references instead of hardcoded hex/rgba"
      contains: "var(--header-bg)"
    - path: "frontend/src/App.tsx"
      provides: "Root element with data-theme='dark' attribute and useEffect for html element"
      contains: "data-theme"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/styles/global.css"
      via: "data-theme attribute triggers [data-theme='dark'] CSS selector"
      pattern: "data-theme.*dark"
    - from: "CSS component rules"
      to: ":root / [data-theme] token blocks"
      via: "var() references"
      pattern: "var\\(--"
---

<objective>
Replace all hardcoded colors in global.css component rules with var() token references, and wire the dark theme as default in App.tsx.

Purpose: Complete the THEME-01 requirement by making every color in the CSS file theme-aware and activating dark mode as default.
Output: Zero hardcoded colors outside token definition blocks in global.css; App.tsx renders with `data-theme="dark"`.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-theme-token-system/02-CONTEXT.md
@.planning/phases/02-theme-token-system/02-RESEARCH.md
@.planning/phases/02-theme-token-system/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output: token names available for use -->
Tokens defined in :root and [data-theme="dark"]:
--header-bg, --header-bg-mid, --header-bg-mid2, --header-bg-end, --header-shadow
--primary-hover-bg, --primary-hover-border, --primary-focus-ring
--primary-shadow, --primary-shadow-hover
--accent-shadow, --accent-shadow-hover, --accent-shadow-strong, --accent-shadow-stronger
--resize-handle-hover, --table-row-hover
--success-bg, --success-bg-end, --success-border
--warning-bg, --warning-bg-end, --warning-border
--danger-bg, --danger-bg-end, --danger-border
--info-bg, --info-bg-end, --info-text
--overlay-light, --overlay-subtle, --stepper-active-overlay
--popup-bg, --popup-shadow, --close-btn-color, --close-btn-hover
--footer-text, --surface-elevated

<!-- From App.tsx: current root element -->
```tsx
<div className="app-container" style={{ display: 'flex' }}>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all hardcoded colors in global.css with var() token references</name>
  <files>frontend/src/styles/global.css</files>
  <read_first>
    - frontend/src/styles/global.css (full file — must see every hardcoded color in component rules)
    - .planning/phases/02-theme-token-system/02-RESEARCH.md (Hardcoded Color Inventory table, lines 448-482)
  </read_first>
  <action>
Replace every hardcoded hex, rgba, and named color value in CSS component rules (everything BELOW the :root and [data-theme="dark"] token blocks) with the corresponding var() reference. Use the complete inventory from the research doc:

**Header section (~line 49-55 after token blocks shift):**
- `.header` background gradient: `linear-gradient(135deg, #b71c1c 0%, #c62828 40%, #d32f2f 70%, #e53935 100%)` -> `linear-gradient(135deg, var(--header-bg) 0%, var(--header-bg-mid) 40%, var(--header-bg-mid2) 70%, var(--header-bg-end) 100%)`
- `.header` box-shadow: `rgba(183, 28, 28, 0.3)` -> `var(--header-shadow)`
- Note: `.header-icon` filter drop-shadow `rgba(0, 0, 0, 0.3)` is theme-invariant black shadow — KEEP as-is

**Nav link hover (~line 101-102):**
- `.nav-link:hover` background: `rgba(198, 40, 40, 0.04)` -> `var(--primary-hover-bg)`
- `.nav-link:hover` border-left-color: `rgba(198, 40, 40, 0.3)` -> `var(--primary-hover-border)`

**Input focus (~line 215):**
- `.input-enhanced:focus` box-shadow: `rgba(198, 40, 40, 0.12)` -> `var(--primary-focus-ring)`

**Button primary (~line 250, 255):**
- `.btn-primary` box-shadow: `rgba(198, 40, 40, 0.25)` -> `var(--primary-shadow)`
- `.btn-primary:hover` box-shadow: `rgba(198, 40, 40, 0.35)` -> `var(--primary-shadow-hover)`

**Button accent (~line 283, 287):**
- `.btn-accent` box-shadow: `rgba(21, 101, 192, 0.25)` -> `var(--accent-shadow)`
- `.btn-accent:hover` box-shadow: `rgba(21, 101, 192, 0.35)` -> `var(--accent-shadow-hover)`

**Result boxes (~lines 312-340):**
- `.result-box.success` background/gradient: `#e8f5e9` -> `var(--success-bg)`, `#c8e6c9` -> `var(--success-bg-end)`, border `#a5d6a7` -> `var(--success-border)`
- `.result-box.warning` background/gradient: `#fff3e0` -> `var(--warning-bg)`, `#ffe0b2` -> `var(--warning-bg-end)`, border `#ffcc80` -> `var(--warning-border)`
- `.result-box.error` / `.error-message` background: `#ffebee` -> `var(--danger-bg)`, `#ffcdd2` -> `var(--danger-bg-end)`, border `#ef9a9a` -> `var(--danger-border)`
- `.instructions-box` background: `#e3f2fd` -> `var(--info-bg)`, `#bbdefb` -> `var(--info-bg-end)`, text color `#1a237e` -> `var(--info-text)`
- `.instructions-box code` background: `rgba(255, 255, 255, 0.6)` -> `var(--overlay-light)`

**Stepper (~lines 394-420):**
- `.step.active` box-shadow: `rgba(198, 40, 40, 0.2)` -> `var(--primary-shadow)` (close enough, or use --primary-focus-ring)
- `.step.done` background: `#e8f5e9` -> `var(--success-bg)`, border `#a5d6a7` -> `var(--success-border)`
- `.step-number` background: `rgba(0, 0, 0, 0.08)` -> `var(--overlay-subtle)`
- `.step.active .step-number` background: `rgba(255, 255, 255, 0.25)` -> `var(--stepper-active-overlay)`

**Prefilled banner (~line 472):**
- Background gradient using `#e8f5e9`, `#c8e6c9` -> `var(--success-bg)`, `var(--success-bg-end)`

**Table row hover (~line 530):**
- `rgba(198, 40, 40, 0.02)` -> `var(--table-row-hover)`

**Sidebar toggle (~lines 585, 595):**
- Box-shadow `rgba(21, 101, 192, 0.4)` -> `var(--accent-shadow-strong)`
- Hover box-shadow `rgba(21, 101, 192, 0.5)` -> `var(--accent-shadow-stronger)`

**AI popup (~lines 620-643):**
- Background: `white` -> `var(--popup-bg)`
- Box-shadow: `rgba(0,0,0,0.16)`, `rgba(0,0,0,0.08)` -> `var(--popup-shadow)`
- Close button color: `#999` -> `var(--close-btn-color)`
- Close button hover: `#333` -> `var(--close-btn-hover)`

**Sidebar panel shadow (~line 654):**
- `rgba(0, 0, 0, 0.08)` -> `var(--overlay-subtle)` (or keep since it is a generic shadow)

**Resize handle (~line 676):**
- `rgba(21, 101, 192, 0.25)` -> `var(--resize-handle-hover)`

**Workflow number active (~line 804):**
- `rgba(198, 40, 40, 0.2)` -> `var(--primary-shadow)` (reuse)

**Footer (~line 815):**
- `rgba(255, 255, 255, 0.7)` -> `var(--footer-text)`

After all replacements, verify with: `grep -n '#[0-9a-fA-F]\{3,8\}' frontend/src/styles/global.css | grep -v ':root' | grep -v 'data-theme' | grep -v 'font-display\|url('`

The only remaining hardcoded colors should be: (1) the Google Fonts import URL, (2) `color: white` on header text (theme-invariant — header always has white text), and (3) the `rgba(0, 0, 0, 0.3)` drop-shadow on header icon (theme-invariant black shadow). All others must use var() references.

Keep all `!important` declarations where they exist, just replace the hardcoded value with `var()`.
  </action>
  <verify>
    <automated>cd "/Users/uri/uri_labtools/try1/uri_labtools/Google antigravity /Tau-ly website" && echo "=== Remaining hardcoded hex colors ===" && grep -n '#[0-9a-fA-F]\{3,8\}' frontend/src/styles/global.css | grep -v ':root' | grep -v 'data-theme' | grep -v 'url(' | grep -v 'Google' && echo "=== Remaining rgba outside tokens ===" && grep -n 'rgba(' frontend/src/styles/global.css | grep -v ':root' | grep -v 'data-theme' | grep -v 'var(' && echo "=== var() usage count ===" && grep -c 'var(--' frontend/src/styles/global.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '#[0-9a-fA-F]\{3,8\}' frontend/src/styles/global.css | grep -v ':root' | grep -v 'data-theme' | grep -v 'url('` returns at most 1-2 lines (only `color: white` on header, which is a keyword not hex)
    - `grep -n 'rgba(' frontend/src/styles/global.css | grep -v ':root' | grep -v 'data-theme' | grep -v 'var('` returns at most 1 line (the theme-invariant header icon drop-shadow)
    - global.css contains `var(--header-bg)` (grep returns >= 1)
    - global.css contains `var(--primary-hover-bg)` (grep returns >= 1)
    - global.css contains `var(--success-bg)` (grep returns >= 1)
    - global.css contains `var(--popup-bg)` (grep returns >= 1)
    - global.css contains `var(--footer-text)` (grep returns >= 1)
    - `grep -c 'var(--' frontend/src/styles/global.css` returns >= 80 (tokens defined + var() usages)
  </acceptance_criteria>
  <done>Zero hardcoded color values remain in CSS component rules (outside :root and [data-theme] blocks), except theme-invariant black shadow on header icon and white header text. All ~40 hardcoded colors replaced with var() references.</done>
</task>

<task type="auto">
  <name>Task 2: Wire dark theme default in App.tsx</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx (current root element and imports)
    - .planning/phases/02-theme-token-system/02-RESEARCH.md (App.tsx Root Element Change section and Pitfall 3: Body Background Bleed)
  </read_first>
  <action>
Make two changes to App.tsx:

1. Add `useEffect` import if not already present. The current imports include `{ BrowserRouter as Router, Routes, Route, Link }` from react-router-dom and component imports. Add:
```tsx
import { useEffect } from 'react';
```

2. Inside the `App` function body, before the `return` statement, add a useEffect that sets `data-theme="dark"` on the `<html>` element for full body coverage:
```tsx
useEffect(() => {
    document.documentElement.setAttribute('data-theme', 'dark');
}, []);
```

3. On the root div (currently `<div className="app-container" style={{ display: 'flex' }}>`), add `data-theme="dark"`:
```tsx
<div className="app-container" data-theme="dark" style={{ display: 'flex' }}>
```

This dual approach ensures both the document root (for body background) and the React container have the dark theme attribute. Per Pitfall 3 in the research doc, this prevents light background bleed.

Do NOT change anything else in App.tsx. The existing inline styles on elements are Phase 3 scope.
  </action>
  <verify>
    <automated>cd "/Users/uri/uri_labtools/try1/uri_labtools/Google antigravity /Tau-ly website" && grep -n 'data-theme' frontend/src/App.tsx && grep -n 'documentElement' frontend/src/App.tsx && grep -n 'useEffect' frontend/src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx contains `data-theme="dark"` on the app-container div (grep returns line with both `app-container` and `data-theme`)
    - App.tsx contains `document.documentElement.setAttribute('data-theme', 'dark')` (grep for `documentElement` returns >= 1)
    - App.tsx contains `import { useEffect } from 'react'` or `useEffect` is imported (grep returns >= 1)
    - App.tsx has a `useEffect` call with empty dependency array `[]` (the theme-setting effect)
  </acceptance_criteria>
  <done>App.tsx renders with data-theme="dark" on both the html element and the app-container div. Dark theme is the default. Body background uses dark tokens.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of dark theme</name>
  <files>frontend/src/styles/global.css, frontend/src/App.tsx</files>
  <action>
Present the running app to the user for visual verification of the dark theme. Start the dev server if not already running with `cd frontend && npm run dev`. Then guide the user through the verification steps below. This is a human checkpoint — no code changes needed.
  </action>
  <verify>
    <automated>cd "/Users/uri/uri_labtools/try1/uri_labtools/Google antigravity /Tau-ly website" && cd frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>User confirms dark theme renders correctly with scientific instrument aesthetic across all pages. No jarring light backgrounds, all text readable, header gradient darkened, surface layering visible.</done>
  <what-built>Complete CSS token architecture with dark/light theme sets, all hardcoded colors in global.css replaced with var() token references, and dark theme wired as default in App.tsx. The app should render with a scientific instrument panel aesthetic: deep navy-charcoal backgrounds (#1a1a2e), violet-gray borders (#2a2a4a), soft white text (#e0e0e8), darker red header gradient, and layered surface depth.</what-built>
  <how-to-verify>
    1. Start the dev server: `cd frontend && npm run dev` (or use ./start.sh)
    2. Open http://localhost:5173 in the browser
    3. Verify the following on the Home page:
       - Page background is dark navy (#1a1a2e), not white
       - Header gradient is dark red (muted, not bright)
       - Text is light/white-ish (#e0e0e8), readable
       - Cards/surfaces appear slightly lighter than the page background (layered depth)
       - Borders are subtle violet-gray, not bright
    4. Navigate to AutoLab page — verify result boxes would render with dark backgrounds (may need to run an analysis)
    5. Navigate to any tool page (e.g., Graph Fitting) — verify form inputs, buttons are visible and readable
    6. Check that no element appears with a jarring white/light background (indicating a missed hardcoded color)
    7. Check the header icon and text are still visible (white text on dark red gradient)
    8. Open browser DevTools, inspect any element, verify CSS custom properties are resolving from [data-theme="dark"]
  </how-to-verify>
  <resume-signal>Type "approved" if the dark theme looks correct, or describe any visual issues you see</resume-signal>
</task>

</tasks>

<verification>
1. `grep -n '#[0-9a-fA-F]\{3,8\}' frontend/src/styles/global.css | grep -v ':root' | grep -v 'data-theme' | grep -v 'url(' | wc -l` returns <= 2
2. `grep -c 'data-theme' frontend/src/App.tsx` returns >= 2
3. `grep -c 'var(--' frontend/src/styles/global.css` returns >= 80
4. Visual: app renders in dark theme with scientific instrument aesthetic
</verification>

<success_criteria>
- All hardcoded colors in global.css component rules replaced with var() references (per THEME-01)
- App.tsx has data-theme="dark" on both html element and app-container div
- Dark theme renders by default with D-01 scientific instrument aesthetic
- D-02 anchor colors visible: navy background, dark surface panels, violet-gray borders, soft white text
- D-04 darkened header gradient visible
- D-05 layered surface depth observable (bg lighter than cards lighter than elevated panels)
- No flash of white/light background on page load
</success_criteria>

<output>
After completion, create `.planning/phases/02-theme-token-system/02-02-SUMMARY.md`
</output>
