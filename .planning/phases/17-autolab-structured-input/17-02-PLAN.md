---
phase: 17-autolab-structured-input
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - frontend/src/components/AutoLab.tsx
autonomous: false
requirements: [SI-07, SI-08]

must_haves:
  truths:
    - "Report context form (title, subject, equipment, notes) appears below results after analysis completes"
    - "Example dataset buttons pre-fill column assignments, axis labels, model, formula, and theoretical value"
    - "Example dataset Run produces correct results without user editing any field"
  artifacts:
    - path: "frontend/src/components/AutoLab.tsx"
      provides: "Updated EXAMPLE_DATASETS + report section positioning"
      min_lines: 800
  key_links:
    - from: "loadExample()"
      to: "structured form state"
      via: "setState calls for xCol, yCol, xErrCol, yErrCol, xLabel, yLabel, selectedModel, formulaExpr, theoVal, theoUnc"
      pattern: "setXCol|setYCol|setSelectedModel"
---

<objective>
Update example datasets to pre-fill the structured form and position the report section correctly post-analysis.

Purpose: Complete the structured input experience -- examples demonstrate the full flow without requiring users to figure out column assignments. Report context form stays clean and appears only after results (per D-07).
Output: EXAMPLE_DATASETS updated with structured fields. Report section positioned per D-07. Visual verification of full end-to-end flow.
</objective>

<execution_context>
@/Users/uri/uri_labtools/try1/uri_labtools/Google antigravity /Tau-ly website/.claude/get-shit-done/workflows/execute-plan.md
@/Users/uri/uri_labtools/try1/uri_labtools/Google antigravity /Tau-ly website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-autolab-structured-input/17-CONTEXT.md
@.planning/phases/17-autolab-structured-input/RESEARCH.md
@.planning/phases/17-autolab-structured-input/17-01-SUMMARY.md

@frontend/src/components/AutoLab.tsx

<interfaces>
<!-- EXAMPLE_DATASETS current shape (from Plan 01 refactored code) -->
Current dataset has: label, instructions (REMOVED in plan 01), theoVal, theoUnc, columns, rows, reportContext, titlePage

New shape needed:
```typescript
{
  label: string;
  xCol: string;
  yCol: string;
  xErrCol: string;       // "None" if not applicable
  yErrCol: string;
  xLabel: string;
  yLabel: string;
  model: string;          // "linear" | "quadratic" | etc.
  customExpr?: string;    // only when model="custom"
  formulaExpr: string;    // "" if none
  theoVal: string;        // "" if none
  theoUnc: string;        // "" if none
  columns: string[];
  rows: Record<string, any>[];
  reportContext: { title: string; subject: string; equipment: string; notes: string };
  titlePage: { studentName: string; studentId: string; labPartner: string; labPartnerId: string; courseName: string; experimentTitle: string; date: string };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Examples + report position</name>
  <files>frontend/src/components/AutoLab.tsx</files>
  <action>
**Update EXAMPLE_DATASETS (per D-08):**

Replace the Free Fall dataset entry. Remove the `instructions` field. Add structured fields:

```typescript
const EXAMPLE_DATASETS = [
  {
    label: 'Free Fall (Quadratic)',
    xCol: 'Time_s',
    yCol: 'Height_m',
    xErrCol: 'Time_Error_s',
    yErrCol: 'Height_Error_m',
    xLabel: 'Time [s]',
    yLabel: 'Height [m]',
    model: 'quadratic',
    formulaExpr: '2*a',
    theoVal: '9.81',
    theoUnc: '0.01',
    columns: ['Time_s', 'Height_m', 'Height_Error_m', 'Time_Error_s'],
    rows: [ /* keep existing rows unchanged */ ],
    reportContext: { /* keep existing */ },
    titlePage: { /* keep existing */ },
  },
];
```

**Update loadExample function:**
Replace the current `loadExample` to set all structured form fields:

```typescript
const loadExample = (ex: typeof EXAMPLE_DATASETS[0]) => {
  // Set file from example data
  const f = rowsToFile(ex.columns, ex.rows, `${ex.label.replace(/\s+/g, '_')}.csv`);
  setFile(f);
  setPreviewData({ columns: ex.columns, rows: ex.rows });
  setSheetNames([]);
  setSelectedSheet('');

  // Set structured form fields (per D-08)
  setXCol(ex.xCol);
  setYCol(ex.yCol);
  setXErrCol(ex.xErrCol);
  setYErrCol(ex.yErrCol);
  setXLabel(ex.xLabel);
  setYLabel(ex.yLabel);
  setSelectedModel(ex.model);
  setFormulaExpr(ex.formulaExpr);
  setTheoVal(ex.theoVal);
  setTheoUnc(ex.theoUnc);
  setCustomExpr(ex.model === 'custom' ? (ex.customExpr || '') : '');
  setFixedParams({});

  // Set demo report context
  setDemoReportContext(ex.reportContext);

  // Clear previous results
  setFitResult(null);
  setFormulaResult(null);
  setNsigmaResult(null);
  setSummaryText(null);
  setError('');
};
```

**Example datasets button text (per D-08):**
Update the label text from "Try an example (loads data + instructions)" to "Try an example (loads data + form fields)".

**Report section position (per D-07):**
Verify that `ReportExpander` with `ReportSection` inside appears BELOW all results (summary, parameter table, formula, n-sigma, plots, export buttons). It should already be at the bottom from Plan 01 -- if not, move it to be the last element inside the results section. The report section should only be visible when `fitResult` is truthy (i.e., after analysis completes).

**Subtitle update:**
Change the AutoLab subtitle from "AI-Powered Automated Analysis -- Upload, Instruct, Get Results" to "Automated Analysis -- Upload, Configure, Get Results" (since it's no longer AI-driven for the main pipeline).
  </action>
  <verify>
    <automated>cd "/Users/uri/uri_labtools/try1/uri_labtools/Google antigravity /Tau-ly website/frontend" && npx tsc -b --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>EXAMPLE_DATASETS uses structured fields (xCol, yCol, model, formulaExpr, etc). loadExample pre-fills all form fields. Report section positioned below results. TypeScript compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification</name>
  <files>frontend/src/components/AutoLab.tsx</files>
  <action>
Verify the complete structured input flow end-to-end. This is a human verification checkpoint -- no code changes.
  </action>
  <verify>
    <automated>cd "/Users/uri/uri_labtools/try1/uri_labtools/Google antigravity /Tau-ly website/frontend" && npx tsc -b --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>User confirms: structured form works, example pre-fills correctly, direct API calls verified in network tab, results render, export works, report section appears post-analysis.</done>
  <what-built>Complete structured input flow: form with column pickers, axis labels, model selector, formula input, theory inputs. Direct API calls (no AI orchestrator). Example dataset pre-fills all fields. Report section post-analysis.</what-built>
  <how-to-verify>
    1. Run `cd frontend && npm run dev` (or use start.sh)
    2. Open http://localhost:5173/autolab
    3. Verify NO free-text instructions textarea exists
    4. Click "Free Fall (Quadratic)" example button
    5. Verify form is pre-filled: X=Time_s, Y=Height_m, X-err=Time_Error_s, Y-err=Height_Error_m, Model=quadratic, Formula=2*a, Theo=9.81/0.01
    6. Click "Run Analysis"
    7. Verify Network tab: calls go to /api/fitting/fit, /api/formula/evaluate, /api/nsigma/calculate, /api/autolab/chat (NOT /api/autolab/run)
    8. Verify results: parameter table shows a/b/c with uncertainties, formula shows g value, n-sigma comparison shows green (consistent), summary text appears, fit+residuals plots render
    9. Verify report expander appears BELOW results
    10. Verify export (DOCX) still works
    11. Upload your own CSV/Excel file -- verify column dropdowns auto-detect correctly and show ALL columns
    12. Verify model dropdown has no "Auto (AI chooses)" option
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Example data -> form | Hardcoded data, no trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Spoofing | example datasets | accept | Hardcoded in source, no external input |
</threat_model>

<verification>
1. `cd frontend && npx tsc -b --noEmit` -- zero type errors
2. `cd frontend && npm run build` -- production build succeeds
3. Click Free Fall example -> all structured fields pre-filled correctly
4. Run Analysis on example -> full results (parameters, formula g=2a, n-sigma, summary, plots)
5. Report expander visible below results, functional
6. Upload custom file -> column auto-detect works, all columns in dropdowns
</verification>

<success_criteria>
- Example datasets pre-fill structured form fields (D-08)
- Report context form is post-analysis section (D-07)
- Full end-to-end flow works: example -> Run -> results -> export
- No reference to free-text instructions anywhere
</success_criteria>

<output>
After completion, create `.planning/phases/17-autolab-structured-input/17-02-SUMMARY.md`
</output>
