---
phase: 9
slug: report-data-contract-and-file-parsing
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-24
---

# Phase 9 -- UI Design Contract

> Visual and interaction contract for the report data contract and file parsing phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (custom CSS with design tokens) |
| Icon library | none (emoji used inline, consistent with existing codebase) |
| Font | Inter (Google Fonts, weights 300-800 loaded, 400+600 used in this phase) |

**Note:** This project uses a custom design token system defined in `frontend/src/styles/global.css` with `:root` (light) and `[data-theme="dark"]` token sets. All new UI must use existing `var(--token)` references. No shadcn, no Tailwind, no component library.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing, small gaps |
| md | 16px | Default element spacing, form group gaps |
| lg | 24px | Section padding (matches existing `1.5rem` pattern in ReportBeta.tsx) |
| xl | 32px | Layout gaps, page margin (`2rem` pattern in ReportBeta.tsx) |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions: none

**Implementation note:** Existing codebase uses `rem` units with 16px base (`1rem = 16px`, `1.5rem = 24px`, `2rem = 32px`). New code should continue using `rem` for consistency. The spacing scale maps: xs=0.25rem, sm=0.5rem, md=1rem, lg=1.5rem, xl=2rem.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px (1rem) | 400 | 1.6 |
| Label | 14px (0.875rem) | 400 | 1.4 |
| Heading | 20px (1.25rem) | 600 | 1.3 |
| Page Title | 28px (1.75rem) | 600 | 1.2 |

Declared weights: 400 (regular), 600 (semibold). Two weights only.

**Source:** Matches existing patterns in ReportBeta.tsx (`fontSize: '1.1rem'` for subheadings, `fontWeight: 600` for emphasis). Normalized to 4 size tiers and 2 weights. All inline sizes in component specs below reference one of these 4 tiers -- no ad-hoc sizes permitted.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `var(--bg)` / `var(--surface)` | Page background `#f8f9fc`, card/section surfaces `#ffffff` |
| Secondary (30%) | `var(--surface-alt)` / info palette | Upload dropzone background `var(--info-bg)` = `#e3f2fd`, extracted text textarea background `var(--surface-alt)` = `#f4f5f9` |
| Accent (10%) | `var(--primary)` = `#1565c0` | Upload dropzone border, upload CTA text, section headings |
| Destructive | `var(--danger)` = `#d32f2f` | Not used in this phase (no destructive actions) |
| Warning | `var(--warning)` = `#f57c00` | Scanned PDF warning banner |
| Success | `var(--success)` = `#2e7d32` | File successfully uploaded indicator |

Accent reserved for: upload dropzone dashed border, "Upload" CTA label text, instruction file section heading color, active drag-over state border.

**Dark theme:** All colors reference `var(--*)` tokens which auto-switch via `[data-theme="dark"]`. No hardcoded hex values allowed in new code.

---

## Component Inventory

This phase adds two visual components to ReportBeta.tsx. All other work is data-layer (TypeScript interfaces, Python functions) with no visual output.

### 1. Instruction File Upload Dropzone

**Location:** ReportBeta.tsx, below the existing "Coming Soon" info card.

**Visual spec:**
- Container: `border: 2px dashed var(--primary)`, `border-radius: 10px`, `padding: 1.5rem`, `text-align: center`, `cursor: pointer`, `background: var(--info-bg)`
- Matches AutoLab.tsx dropzone exactly (line 558-562 pattern)
- Label above: "Lab Instructions (Optional)" at Label tier (14px/0.875rem, weight 400)
- Sub-label: "Upload your lab instruction file for AI context" at Body tier (16px/1rem), `color: var(--text-secondary)`

**States:**

| State | Visual |
|-------|--------|
| Empty (default) | Dashed blue border, light blue background `var(--info-bg)`, text: "Click or drag & drop your instruction file" + `<small>.pdf, .docx</small>` in `color: var(--primary)` |
| Drag hover | Border becomes solid (not dashed), background lightens slightly -- `var(--primary-hover-bg)` overlay |
| File selected | Background changes to `var(--success-bg)` = `#e8f5e9`, text shows filename + size in `color: var(--success)`, weight 600 |
| Uploading | Show inline "Extracting text..." with a simple CSS spinner (no library), background stays `var(--info-bg)` |
| Error | Red text below dropzone: error message in `color: var(--danger)`, Label tier (14px/0.875rem) |

**Interactions:**
- Click anywhere in dropzone opens native file picker (`accept=".pdf,.docx"`)
- Drag-and-drop: `onDragOver` prevents default, `onDrop` reads first file
- Re-upload replaces previous file (D-03)
- Hidden `<input type="file">` triggered by ref (same pattern as AutoLab.tsx line 575-579)

### 2. Extracted Text Confirmation Textarea

**Location:** Appears below the dropzone after successful text extraction.

**Visual spec:**
- Section heading: "Extracted Text" at Heading tier (20px/1.25rem, weight 600)
- Textarea: `width: 100%`, `min-height: 200px`, `max-height: 400px`, `resize: vertical`
- Textarea styling: `border: 1.5px solid var(--border)`, `border-radius: var(--radius-sm)` (8px), `padding: 1rem`, `background: var(--surface)`, `color: var(--text)`, `font-family: inherit`, `font-size: 0.875rem` (Label tier, 14px), `line-height: 1.6`
- Focus state: `border-color: var(--primary)`, `box-shadow: 0 0 0 3px var(--primary-focus-ring)` (matches existing input focus pattern)
- Helper text below textarea: "Review and edit the extracted text. This will be sent to AI for report generation." in `color: var(--text-secondary)`, Label tier (14px/0.875rem)

**States:**

| State | Visual |
|-------|--------|
| Hidden | Not rendered until text extraction completes |
| Populated | Textarea filled with extracted text, editable |
| Empty (scanned PDF) | Textarea empty, warning banner above it |
| Edited | No visual change -- user edits are preserved in state |

### 3. Scanned PDF Warning Banner

**Location:** Above the textarea, only when extracted text is empty (D-04).

**Visual spec:**
- Uses existing `.alert.warning` pattern: `background: linear-gradient(135deg, var(--warning-bg) 0%, var(--warning-bg-end) 100%)`, `border-color: var(--warning-border)`, `border-radius: var(--radius)` (12px), `padding: 1rem`
- Icon: warning emoji prefix with `aria-hidden="true"` on the emoji `<span>` element so screen readers do not announce the emoji redundantly (the banner text is self-explanatory and the `role="alert"` on the container handles announcement)
- Copy: see Copywriting Contract below

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Section heading | "Lab Instructions" |
| Upload label | "Lab Instructions (Optional)" |
| Upload sub-label | "Upload your lab instruction file for AI context" |
| Dropzone empty | "Click or drag & drop your instruction file" |
| Dropzone file types | ".pdf, .docx" |
| Dropzone file selected | "{filename} ({size} KB)" |
| Dropzone uploading | "Extracting text..." |
| Extracted text heading | "Extracted Text" |
| Extracted text helper | "Review and edit the extracted text. This will be sent to AI for report generation." |
| Textarea placeholder | "Extracted text will appear here, or type/paste your lab instructions manually..." |
| Scanned PDF warning | "No text could be extracted -- this may be a scanned or image-based PDF. You can type or paste the instructions manually below." |
| Upload error (unsupported type) | "Unsupported file type. Please upload a PDF or DOCX file." |
| Upload error (server) | "Failed to extract text. Please try again or paste the instructions manually." |
| Upload error (too large) | "File is too large. Please upload a file under 10 MB." |
| Empty state heading | "Coming Soon" (existing -- no change) |
| Empty state body | Existing ReportBeta.tsx copy -- no change this phase |
| Primary CTA | None in this phase (no submit/generate button yet -- that is Phase 10+) |
| Destructive confirmation | None in this phase (no destructive actions) |

---

## Layout

**Page structure (ReportBeta.tsx after this phase):**

```
[Page container: maxWidth 700px, margin 2rem auto, padding 2rem]
  |
  +-- [Header: "Lab Report Export" + Beta badge]  (existing, unchanged)
  |
  +-- [Info card: "Coming Soon" description]  (existing, unchanged)
  |
  +-- [NEW: Lab Instructions section]
  |     +-- Section heading: "Lab Instructions (Optional)"
  |     +-- Sub-label text
  |     +-- Upload dropzone (drag-and-drop + click)
  |     +-- [Conditional: Scanned PDF warning banner]
  |     +-- [Conditional: Extracted text textarea + helper text]
  |
  +-- [Infrastructure Preview card + Test PDF button]  (existing, unchanged, D-14)
```

**Spacing between sections:** 24px (`1.5rem`) -- matches existing `marginBottom: '1.5rem'` in ReportBeta.tsx.

---

## Interaction States Summary

| Interaction | Trigger | Visual Feedback | Duration |
|-------------|---------|-----------------|----------|
| File drag over dropzone | dragover event | Border becomes solid, subtle background shift | Immediate |
| File drop | drop event | Transitions to "Extracting text..." state with spinner | Until API responds |
| File click-to-browse | Click on dropzone | Native file picker opens | N/A |
| Text extraction success | API returns text | Dropzone shows filename+size (green), textarea appears below with text | Immediate |
| Text extraction empty | API returns empty text + warning | Warning banner + empty textarea appears | Immediate |
| Text extraction error | API returns error | Error text below dropzone in red | Immediate |
| Textarea edit | User types | Standard textarea behavior, no special feedback | Immediate |
| Re-upload | Drop or select new file | Replaces filename, re-triggers extraction, updates textarea | Until API responds |

---

## Accessibility

- Dropzone has `role="button"` and `tabIndex={0}` for keyboard access
- Dropzone responds to Enter/Space keypress (triggers file picker)
- Textarea has associated `<label>` via `htmlFor`/`id`
- Warning banner uses `role="alert"` for screen reader announcement
- Warning banner emoji wrapped in `<span aria-hidden="true">` to prevent redundant screen reader announcement
- Error messages use `aria-live="polite"` region
- All interactive elements have visible focus indicators via existing `var(--primary-focus-ring)` pattern

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| N/A | none | not applicable |

No component libraries or third-party registries are used. All UI is custom HTML elements styled with CSS custom properties.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
